# Run-plan 2026-08-11 — foreman day (PAUSED after Session 1, limit reset)

Her brief: (1) admin dashboard grouped by chapter with dates, (2) build the hersiening
rounds = the queued Deel 2 siblings, (3) archive the algebra chapters (ch1+ch2) — the
class isn't doing algebra any more this year.

## Day queue

| # | What | State |
|---|---|---|
| 1 | Archive ch1+ch2 + admin dashboard regroup (front-end only) | ✅ BUILT + foreman-reviewed GREEN (commit ff33a89) |
| 2 | Deel 2 ch3: m1b–m10b | prompt below, NOT dispatched |
| 3 | Deel 2 ch4: s1b–s10b | prompt below, NOT dispatched |
| 4 | Deel 2 ch5: t1b–t10b + write migration-deel2.sql | prompt below, NOT dispatched |

Sunday's Sessions 7–8 (u/v b-rounds) are DROPPED — no revision rounds for archived
chapters. migration-deel2.sql therefore seeds **30** b-round ids (m/s/t only), not 45.

Session 1 review evidence (2026-08-11): hub starts at Meetkunde, archived chapters
unreachable (renderChapter + renderPlay guards; app has no URL routes so in-app paths are
the only paths); admin "📦 Argief" fold works with live checkboxes; learner grid clusters
per chapter, chip tooltip carries best% + "gespeel <datum>" (last_played_at was already in
the adminData payload — front-end-only fix, NO migration); CSV switched to round ids +
last-played column; phone width contained; 0 console errors; local backend stores
last_played_at too so ?local=1 shows dates.

⚠️ OPEN QUESTION for Megan: Session 1 also hid the "Where the class is stuck" admin
section (js/admin.js `SHOW_STRUGGLES = false`, section still fully built) with a comment
claiming it was her call that day. Not in the foreman brief — confirm she asked the build
session for it, or flip the flag back to true.

## Session 2 prompt (paste into a fresh Sonnet session)

```
SESSION 2 — Maths Quest Gr7: Deel 2 for Hoofstuk 3 (Reguitlyn Meetkunde), m1b–m10b

Project: C:\Users\megzi\Desktop\Claude Code Projects\maths-quest-grade7
Read CLAUDE.md, PROJECT-STATUS.md and RUN-PLAN-2026-08-10.md first. This brief is your
full scope — work freely inside it, refuse anything outside it. Megan approved this work
2026-08-11.

CONTEXT: chapters 1–2 (Uitdrukkings/Vergelykings) were ARCHIVED earlier today — do not
touch them and do not build u/v b-rounds. You are building revision siblings for ch3 only.

TASK — every ch3 round m1–m10 gets a "Deel 2" sibling m1b–m10b:
- js/config.js: each rXb titled "<oorspronklike titel> — Deel 2", listed DIRECTLY after
  its sibling; n stays sequential; questCount auto-counts. New gen code goes in
  js/quests/ch3-meetkunde.js (or a sibling file registered in js/quests/index.js).
- Deel 2 = genuinely NEW question styles: reverse the direction (answer→setup), new
  contexts, new figure variants. NEVER a Deel 1 template with fresh numbers. Where Deel 1
  is identification, flip the task.
- m1b = a fresh gradeboog round. The dual-scale protractor is sacred — use
  js/engine/protractor.js as-is, do NOT touch its scales.

HOUSE RULES that bite here: learner content Afrikaans and EASY (don't escalate
difficulty); "Tap" never "tik"/"klik"; marked angles ≥ 25°, no flat triangles (apex
30–120°); tips/hints NEVER reveal the answer or contain the answer's number — the Wenk
is tappable BEFORE answering; refleks/inspringende hoek = 360 − kleiner hoek; forced
skill mixes for answer-determining variants (st20/st24 pattern — list each variant as
its own skill entry, no coin-flips inside one gen). No SQL, no schema.sql, no sw.js,
no git push.

VERIFY before reporting back: rounds with measurable figures carry _chk metadata and are
registered in tools/verify-stellings (extend the harness to these b-rounds); run
`node tools/verify-stellings.mjs` → 0 mismatches required. Non-figure rounds: DOM-play
≥3 questions each at ?local=1 (click via el.dispatchEvent(new MouseEvent('click',
{bubbles:true}))). 0 console errors. Commit with a clear message, NO push, and end your
report by listing every file you changed and what the verify run showed.
```

## Session 3 prompt

Same as Session 2 with these swaps:
- Header: "SESSION 3 — … Hoofstuk 4 (2D Vorms), s1b–s10b".
- CONTEXT adds: build b-siblings for s1–s10 ONLY — NOT s11/s12 (they're brand new drills).
- Gen code home: js/quests/ch4-vorms.js. No protractor line.
- Extra house rules: tap-zone rounds keep [data-tap] semantics; decorative marks
  (pyltjies/merkies/blokkies) never move geometry; congruence stays first-exposure
  pick-one-of-two easy.

## Session 4 prompt

Same as Session 2 with these swaps:
- Header: "SESSION 4 — … Hoofstuk 5 (Transformasies), t1b–t10b".
- Gen code home: js/quests/ch5-transformasies.js. No protractor line.
- t1b inherits the known pigeonhole wording repeat — acceptable, documented in CLAUDE.md.
- PLUS: write (do NOT run) supabase/migration-deel2.sql seeding the 30 b-round ids
  (m1b–m10b, s1b–s10b, t1b–t10b) is_open = TRUE, on conflict do nothing, sort bands 300+,
  in migration-teacher-dashboard.sql style. ONLY m/s/t — u/v b-rounds don't exist.

## Ship plan (Fable session, her explicit go-ahead required)

1. Review pass green on Sessions 2–4 (diffs + verify-stellings + ?local=1 playthroughs).
2. Run supabase/migration-deel2.sql against "Homework Hub" (ref wjkhedepwnwrqcpxmkds) —
   additive, idempotent.
3. git push (Pages deploy; sw auto-updates, no cache bump needed).
4. Live verify: hub shows no algebra chapters; admin grid grouped with dates; play one
   b-round as a test learner; b-rounds show OPEN.
5. Update PROJECT-STATUS.md and commit.

Note: Session 1's work (archive + dashboard) is committed but NOT pushed — it rides along
with the Deel 2 ship, or can ship alone earlier if she asks.
